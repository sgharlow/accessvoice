server {
    listen 80;
    server_name _;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 256;
    gzip_vary on;

    # Allow large audio uploads
    client_max_body_size 20m;

    # Frontend static files
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;

        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }

    # Socket.IO proxy to backend
    location /socket.io/ {
        proxy_pass http://backend:8000/socket.io/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket timeouts — voice sessions can be long-lived
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
        proxy_connect_timeout 60;
    }

    # Backend health check
    location /health {
        proxy_pass http://backend:8000/health;
        proxy_set_header Host $host;
    }
}

# SSL configuration — uncomment after running certbot
# server {
#     listen 443 ssl http2;
#     server_name accessvoice.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/accessvoice.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/accessvoice.yourdomain.com/privkey.pem;
#
#     # Modern SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers off;
#
#     # Include the same location blocks from above
# }
#
# Redirect HTTP to HTTPS — uncomment with SSL
# server {
#     listen 80;
#     server_name accessvoice.yourdomain.com;
#     return 301 https://$host$request_uri;
# }
